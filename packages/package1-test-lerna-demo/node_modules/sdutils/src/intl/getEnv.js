/**
 * @file 获取当前国际化语言环境
 * @returns {string}  结果
 */

export default function getEnv(){
    var locale = JSON.parse(localStorage.LOCALE || '{}');
    var language = locale.language || 'zh-cn';
    return language.toString();     // 'en-us, zh-cn, zh-tw'
 };